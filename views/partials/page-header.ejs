<!-- Start Page Header -->
<section class="page-header">
	<div class="container">
		<span class="page-header-eyebrow"><%= eyebrow || 'Titans Club' %></span>
		<h1><%= title %></h1>
		<% if (typeof subtitle !== 'undefined' && subtitle) { %>
			<p><%= subtitle %></p>
		<% } %>
	</div>
</section>
<!-- End Page Header -->
