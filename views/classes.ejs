<%- include('partials/head', { pageTitle, metaDescription }) %>
<%- include('partials/header', { site, activeNav }) %>

<%- include('partials/page-header', {
	eyebrow: 'Train With Us',
	title: 'Our Classes',
	subtitle: 'Instructor-led sessions designed to build strength, flexibility, and endurance.'
}) %>

<!-- Start Classes -->
<section class="classes" id="classes">
	<div class="container">
		<div class="content">
			<div class="box img wow slideInLeft">
				<img src="/images/class2.png" alt="classes" />
			</div>
			<div class="box text wow slideInRight">
				<h2>Our Classes</h2>
				<p>Discover a variety of fitness classes designed to keep you active, motivated, and progressing toward your goals. Whether you want to improve flexibility, build strength, burn calories, or enhance endurance, our instructor-led classes offer an enjoyable and effective way to stay fit.</p>
				<div class="class-items">
					<% site.classes.forEach(function(item, i) { %>
						<div class="item wow bounceInUp">
							<% if (i % 2 === 0) { %>
								<div class="item-img">
									<img src="/images/<%= item.image %>" alt="<%= item.name %>" />
									<div class="price"><%= item.price %></div>
								</div>
								<div class="item-text">
									<h4><%= item.name %></h4>
									<p><%= item.text %></p>
									<a href="/schedule">Get Details</a>
								</div>
							<% } else { %>
								<div class="item-text">
									<h4><%= item.name %></h4>
									<p><%= item.text %></p>
									<a href="/schedule">Get Details</a>
								</div>
								<div class="item-img">
									<img src="/images/<%= item.image %>" alt="<%= item.name %>" />
									<div class="price"><%= item.price %></div>
								</div>
							<% } %>
						</div>
					<% }); %>
				</div>
			</div>
		</div>
	</div>
</section>
<!-- End Classes -->

<div class="classes-cta">
	<a href="/schedule" class="btn">View Weekly Schedule</a>
</div>

<%- include('partials/footer', { site }) %>
<%- include('partials/scripts') %>
